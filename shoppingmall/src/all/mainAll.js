import { Main } from "../components/main/main";

const Main = ({product, setProduct}) => {
    return (
        <Main
            product = {product}
            setProduct = {setProduct}
        />
    );
};

export default Main;