import { Detail } from "../components/detail/detail";

const ProductAll = ({cart, setCart}) => {
    return (
    <Detail
        cart = {cart}
        setCart = {setCart}

    />);
};

export default ProductAll;